# Segredos e ambiente local (nunca subir ao GitHub)
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Gerado no build local/Vercel — chaves injetadas
firebase-env.js

# Dependências e deploy
node_modules/
.vercel/
dist/
*.log
npm-debug.log*

# Credenciais / backups locais
FIREBASE/
FIREBASE CHAVE KEY.txt
chave api/
*.backup.html
backup/
*.rar
HM AUTOMOTIVO/

# Sistemas base (não fazem parte do deploy HM)
SISTEMA BALCAO.html
RELATORIO VEÍCULOS.html

# SO / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Backup do index monolítico e script de split (só local)
_origem/
_split_hm.js

# Audios/videos locais do cliente (nao versionar)
AUDIOS THIAGO/

