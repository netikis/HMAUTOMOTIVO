{
  "name": "hm-automotivo",
  "private": true,
  "version": "1.0.0",
  "description": "HM Centro Automotivo — gestão local + nuvem Firebase",
  "scripts": {
    "build": "node scripts/build-firebase-env.js",
    "vercel-build": "node scripts/build-firebase-env.js"
  }
}
